services:
  - type: web
    name: xiaowei-workbench
    runtime: node
    plan: free
    branch: main
    buildCommand: "node tools/stamp-sw.js"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /api/ping
